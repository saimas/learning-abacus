# SDD ledger — plan: docs/superpowers/plans/2026-09-20-phase-1-single-rod-anzan.md

## POST-IMPLEMENTATION REQUEST (user, mid-run 2026-09-20): ship to TestFlight, "all the way"

Established path, copied from learning-vocabulary/docs/release-ios.md — MANUAL xcodebuild, NOT EAS (eas is installed but the siblings do not use it for these apps; okkake does, under a different company team 66K7BT6HR6 / admin@us-kikaku.com — not the right account for this).

Credentials, all verified present on this machine:
- ASC API key: ~/.appstoreconnect/private_keys/AuthKey_8LHJ29MNAD.p8 — the ONLY active key; four stale keys sit beside it (2LG2B6F945, 36ZKZT5Q9X, 49ZA7JDK7S revoked, ZNF5S6LK57). If auth fails, check the key first before debugging anything else.
- Key ID 8LHJ29MNAD (Admin role, required for cloud-managed distribution certs)
- Issuer ID 69a6de87-aa14-47e3-e053-5b8c7c11a4d1
- Team ID H7PV4KT858
- Xcode 26.1.1, fastlane, altool all present.

Sequence: bump ios.buildNumber → `CI=1 npx expo prebuild --platform ios` → clean DerivedData + old xcarchive → `xcodebuild archive` with the auth key and DEVELOPMENT_TEAM → **step 3b: verify every @rpath framework the binary links is actually embedded** (a missing one ships and crashes on launch — this happened as vocabulary build 31) → `xcodebuild -exportArchive` with an exportOptions.plist (method app-store-connect, destination upload, teamID H7PV4KT858, signingStyle automatic, manageAppVersionAndBuildNumber false).

app.json conventions from the siblings: bundleIdentifier `com.saimas.<slug>` (learning-database uses com.saimas.dbspdrill), supportsTablet false, non-exempt-encryption flag set so TestFlight skips the export-compliance question per build, and a Japanese CFBundleDisplayName. Proposed for this app: `com.saimas.learningabacus`.

KNOWN GAP: Apple's public App Store Connect API has no endpoint to create an app record. Options, in order: `fastlane produce` (installed — uses the legacy endpoint, may need an Apple ID session rather than the .p8), else the user creates the record in the ASC web UI once. Everything after the record exists is scriptable and mine to do.


Spec: docs/superpowers/specs/2026-09-20-learning-abacus-curriculum-design.md (reachable — rulings are binding, not provisional)
Worktree: .claude/worktrees/phase-1-single-rod-anzan on branch worktree-phase-1-single-rod-anzan
Branch base: c7856df (origin/main)

## Pre-flight scan

### Cross-task rows (shared file or interface)

| Tasks | Produces → consumes | Finding |
|---|---|---|
| 2 → 3 | `RodStep`, `applyStep`, `emptySoroban`, `setValue`, `readValue` | OK |
| 3 → 5,6,7,14 | `ATOMS`, `Atom`, `AtomClass`, `classify`, `decompose` | OK |
| 4 → 5,8,10,14 | `FadeLevel`, `FadeVisual`, `Coaching`, `MAX_FADE`, `visualForFade`, `coachingForFade`, `nextFadeLevel` | OK |
| 5 → 6,7,14 | `AtomRecord`, `isReflex`, `newRecord`, `medianLatencyMs`, `CLASS_TARGET_MS`, `LATENCY_WINDOW`, `applyAttempt` | OK — `isReflex(record, cls, calibrationMs)` arg order identical in 6 and 14 |
| 6 → 7,8 | `atomsForStage`, `highestUnlockedStage`, `StageIndex` | OK |
| 7 → 8,9,12,14 | `Progress`, `emptyProgress`, `recordAttempt`, `markDayPracticed`, `currentStage`, `dayKey`, `SCHEMA_VERSION` | OK |
| 8 → 11,12 | `SessionPlan`, `SessionItem`, `BlockKind`, `MAX_ATTEMPTS_PER_ATOM` | OK |
| 9 → 12 | `loadProgress`, `saveProgress`, `STORAGE_KEY` | OK |
| 10 → 11,13 | `Abacus`, `FadeLayer`, `showsFrame`, `BEAD_OPACITY` | OK |
| 11 → 12 | `AttemptResult`, `SessionRunner` | OK |
| 12 → 13,14,15 | `ProgressProvider`, `useProgress` | OK |
| 1, 12 | both list `app/_layout.tsx`, `app/index.tsx` | **CONFLICT — see R3** |
| 1, 9, 10 | all modify `package.json` | OK — sequential dependency additions, no contention |

### Per-task self-consistency rows

| Task | Tests vs code vs files | Finding |
|---|---|---|
| 1 | Files list names `app/*.tsx`, steps never write them; template choice contradicts `main: expo-router/entry` | **DEFECT — see R2** |
| 2 | 4 rod + 4 soroban tests vs 7 exports | OK |
| 3 | 180-case property test vs `decompose`; worked examples 7+8, 7+6 match the code | OK |
| 4 | 5 visual + 4 coaching + 5 transition cases vs three exports | OK |
| 5 | latency/box/fade tests vs `applyAttempt` | OK |
| 6 | partition + gate tests vs `STAGES` | OK |
| 7 | calibration + day-count tests vs `recordAttempt` | OK |
| 8 | block budget 45+120+90+30 = 285 = `SESSION_SECONDS` | OK |
| 9 | 5 fallback cases vs `loadProgress` try/catch | OK |
| 10 | opacity assertions vs `BEAD_OPACITY` | OK |
| 11 | 3-failure drop test vs `MAX_ATTEMPTS_PER_ATOM` | OK |
| 12 | hydration-gate tests vs provider | OK |
| 13 | `toContain(2)` against string children | **DEFECT — see R5** |
| 14 | `toContain(0)` against string children | **DEFECT — see R5** |
| 15 | `toContain(12)` against string children | **DEFECT — see R5** |

## Pre-flight rulings

Ruling: Scanner's 12 "consumed but never produced" hits are a regex artifact (captured the `type`/`const` keyword instead of the symbol name) — all resolve to an earlier task. No plan change. — verified each by hand against the Produces blocks — cost if wrong: a Task N implementer finds a genuinely missing import and reports NEEDS_CONTEXT.

Ruling: R2 — Task 1 must scaffold with an expo-router template, not `blank-typescript`. The plan's Step 1 uses `--template blank-typescript`, which produces `App.tsx` and no router, yet Step 2 sets `"main": "expo-router/entry"` and Task 12 writes `app/_layout.tsx`. Implementer scaffolds with the default (expo-router) template and deletes the demo screens. — the spec's three-screen structure requires expo-router, matching both sibling apps — cost if wrong: Task 1 rework, one dispatch.

Ruling: R3 — `app/_layout.tsx` and `app/index.tsx` are created as minimal booting placeholders in Task 1 and REPLACED wholesale in Task 12. Task 12's Files entry should read Modify, not Create. Reviewers must not flag the overwrite as scope creep. — the plan's Task 12 code blocks are complete and self-contained — cost if wrong: a spurious review finding, adjudicated in the loop.

Ruling: R4 — `package.json` touched by Tasks 1, 9 and 10 is sequential dependency addition, not a conflict. No action.

Ruling: R5 — Tasks 13, 14 and 15 assert `expect(<string>).toContain(<number>)`, which Jest rejects for a string receiver. Implementers use a string argument instead (e.g. `toContain('0 of 180')`, `toContain('12 days')`, `toContain('Rod 2')`), preserving what each test checks. — the assertion's intent is unambiguous and the fix is mechanical — cost if wrong: a failing test the implementer fixes in place.

## Progress

Task 1: review returned spec ✅ (both R2 and R3 verified against the diff; purity.test.ts, soroban.ts and tsconfig.json confirmed character-exact to the brief; the learning-database mirroring claim independently checked). Two ⚠️ items resolved by the controller:

Ruling: T1-⚠️1 — accept the unverified react-dom/react-native-web peer-conflict claim. The diff exposes no web or android script and no such keys in app.json, so no functional non-iOS path exists either way; the packages sit unused. Not worth an npm install to confirm. — cost if wrong: two unused transitive deps in the tree, removable any time.

Ruling: T1-⚠️2 — accept `npx expo export --platform ios` (1103 modules, no errors) as sufficient boot evidence for Task 1; do NOT require a simulator launch now. R2's "must boot" existed to protect the router entry point after moving routes from src/app/ to app/, and a successful export proves expo-router resolved top-level app/ and the bundle builds. The plan already schedules a real simulator run at Task 15 Step 5. — cost if wrong: a native-layer boot failure surfaces at Task 15 instead of Task 1, after 14 tasks of work.

Task 6: complete (commits d556405..aa96885, review clean). Spec ✅, Approved, zero Critical/Important. Reviewer confirmed allReflex genuinely satisfies isReflex (box 5 clears REFLEX_MIN_BOX, 5 entries clear LATENCY_WINDOW, median 300 < target 900) rather than faking its shape, and that the empty-previous-stage branch is unreachable since all four classes have members.
Task 6: minor (deferred): isStageUnlocked's `previous.length === 0` branch is dead code given the current class distribution.
Task 6: minor (deferred): atomsForStage re-derives all 180 decompositions per call; fine at this scale, revisit only if Tasks 7/8 call highestUnlockedStage per render rather than per progress update.

Task 7: complete (commits eae324f..c5112a6, review clean). Fix round 1/5 (1 addressed, 0 open; commits bc2be32..c5112a6). Re-review confirmed the new test genuinely discriminates (UTC 2026-09-20 vs Tokyo 2026-09-21), that V8 re-reads TZ per local-getter call so the beforeAll scoping is sound, and that only dayKey's body plus the new test changed. 274 tests green.

Task 8: complete (commits c5112a6..130725c, review clean). Fix round 1/5 (1 addressed, 0 open; commits 1041054..130725c). Re-review independently re-derived the four-attempt trace (box 1→5, cc 1→4 never reaching the streak of 5, window full at 3, calibration 900→400, target 540, isReflex true, fade 0) and confirmed both conditional guards are gone from the strengthened test. 284 tests green.

Task 10: complete (commits 90ec126..37e0c86, review clean). Fix round 1/5 (1 addressed, 0 open; commits 5e3e769..37e0c86). Re-review confirmed the caret is gone and the lockfile changed by exactly one line — re-resolved, not regenerated. 302 tests green.

Task 11: complete (commits 37e0c86..5a32d25, review clean). Fix round 1/5 (1 addressed, 0 open; commits 96612b5..5a32d25). Re-review confirmed session-summary now appears on exactly one branch and the new test asserts BOTH session-summary and finish-button with the throwing getByTestId, so a fallback usurping the testID would fail. 315 tests green.

Task 12: complete (commits 5a32d25..f4f5edf, review clean). Fix round 1/5 (2 addressed, 0 open; commits 4423025..f4f5edf). Re-review traced the new ordering — latest.current holds the post-attempt value before attempt() returns, so a synchronous flush() is guaranteed current independent of React's scheduling — and confirmed two same-tick attempts now compose (the second reads what the first wrote), which is strictly better than the old updater form. One caveat it could not settle statically: React's eager-state optimisation can sometimes invoke an updater synchronously, which could in principle have let a single-dispatch test pass pre-fix; it accepted the captured RED output as evidence. 321 tests green.

Task 13: complete (commits f4f5edf..a670d3c, review clean). Spec ✅, Approved, ZERO Critical/Important — no fix round. Reviewer traced the no-loop path independently and went further than asked: verified selectSession never returns null, so app/index.tsx's `plan === null` guard collapses to `!hydrated`, which is what keeps the tutorial branch reachable instead of stranding a fresh learner on the loading screen. Also confirmed the expo-router mock is scoped soundly — the test file imports Today directly, not _layout (which uses Stack) or tutorial (which uses router) — so it cannot mask a regression elsewhere.
Task 13: minor (deferred): saveProgress swallows write errors, so completeTutorial's promise always resolves and navigation proceeds even on a failed write. In-memory state is correct for the session; only a relaunch after a failed write would re-show the tutorial. Inherited from the storage contract (same as reset/flush), not introduced here.

Task 14: complete (commits a670d3c..2246a7c, review clean). Spec ✅, Approved, ZERO Critical/Important — no fix round. Reviewer hand-traced all four cellState fixtures through isReflex/latencyTargetMs and confirmed the mental gate is a genuine conjunction with no fade-alone shortcut, and that the grid maps the full ATOMS array with no filter/slice so a new learner sees the map of what is coming rather than an empty screen.
Task 14: minor → FOLDED INTO TASK 15 (not deferred): grid cells are bare Views with accessibilityLabel but no `accessible={true}`/accessibilityRole. Every other interactive element in this repo (Bead, SessionRunner's buttons) uses Pressable with accessibilityRole="button"; on iOS a bare View's label may not surface to VoiceOver at all. Inherited from the brief's snippet, trivial to fix, and Task 15 is the polish task — cheaper there than in a final-review fix wave.

Task 15: complete (commits 2246a7c..3860e78, review clean). Spec ✅, Approved, ZERO Critical/Important — no fix round. Reviewer verified reachability both ways with no dead ends and confirmed the nav test asserts hrefs by testID so removing the Links would fail it; traced the two-press reset end to end (the two controls are mutually exclusive via ternary, and `reset` is a stable useCallback with no deps, so no re-render or stale-closure path can fire it from one press); and checked the README's atom counts byte-for-byte against the test-enforced distribution rather than taking them on trust.
Task 15: minor (deferred): nav links rely on default Text accessibility rather than an explicit accessibilityRole="link".

FINAL WHOLE-BRANCH REVIEW (opus): verdict NOT READY TO MERGE — 3 Critical, 6 Important, ALL plan defects rather than implementation errors. C1 the fade ladder could not advance (latencyTargetMs('direct',cal) reduced to clamp(cal,540,2250) where cal IS the learner's median, so the gate was "beat your own median" — ~3% per five-streak, impossible above 2250ms; cascade froze 130 of 180 atoms and left the atom map permanently "0 of 180"). C2 sessions delivered 165s not 255s (tail-empty blocks lost their budget; my Task 11 amendment handled leading empties only). C3 the app never taught the method (decompose() called only by classify(); F0 'demo' behaved identically to F1; spec §4's "App demonstrates the move" unimplemented — no task owned it). I1 TestFlight blockers, I2 no flush on backgrounding, I3 Number('')===0 scored blanks CORRECT on the nine n−n atoms, I4 empty close screen, I5 no test composed selectSession with SessionRunner — the seam where C1 and C2 lived, which is why 15 task reviews and 13 fix rounds all passed, I6 tutorial taught nothing.

CONTROLLER ERROR TO OWN: at Task 7 I examined this exact clamp and concluded "the clamp's lower bound is load-bearing, not decorative" — true for cal<540, but I generalised from one end of the range. Across 540-2250ms the target equals the median identically. I looked straight at C1 and missed it.

Ruling: C3 scope — implement the TEXT demonstration, defer bead input to Phase 2. Not scope expansion: spec §4 already defines F0 as demonstrating the move, and decompose() already returns exactly the data (7+8 → [carry +1, working −2]); it was simply never surfaced. Bead-drag input is genuinely new work and the reviewer agreed. The spec's "verify every individual bead movement" claim is therefore DEFERRED, recorded, not quietly unmet. — cost if wrong: Phase 1 ships teaching the method in text rather than by touch.
Ruling: stage re-closing (found BY the fix wave, not a residual) — LATCH the highest stage reached, kept inside the one wave rather than opening a second. isStageUnlocked demanded 85% simultaneously at box>=4 and box resets on any miss, so at day 180 three of four simulated learners read stage 1 while holding 46-49 of 50 atoms at F6, flapping 4-11 times. Material appearing then vanishing is corrosive when visible progress is the only milestone system. — cost if wrong: a learner back from a long break returns to their highest stage rather than a re-tested one; spec §8's recalibration-after-absence is Phase 2.
Ruling: PARKED (Minor, residual after the one fix wave) — MAX_TARGET_MS = 8000 re-inverts the gate for the `both` class once calibrationMs >= 3333ms (uncapped target would be 2.4·cal), and for `direct` at cal >= 8000ms. Same failure mode as C1 at the extreme slow end. Parked rather than fixed: a 3333ms median on DIRECT atoms is very slow, `both` atoms are stage 4 and unreachable until stages 1-3 are cleared (by which point calibration has improved), and spec §12 already flags these targets as needing real-data calibration. Recorded as a Phase 2 calibration item. — cost if wrong: an unusually slow learner's hardest atoms fade more slowly than intended, at a stage they cannot reach for months.

FIX WAVE COMPLETE (10 commits, 3860e78..d62dc65). Re-review: all ten ADDRESSED, no new Critical/Important. Measured, not asserted — C1: 8.6/14.8/17.7 attempts per promotion at 900/2000/4000ms medians (was 95.1, and never above 2250ms); C2: exactly 165,000ms → 255,000ms; latch: day-180 stage 3/3/3/2 versus 2/1/1/1 live. 405 tests across 22 suites.

ALL 15 TASKS COMPLETE. Branch worktree-phase-1-single-rod-anzan, c7856df..3860e78, 338 tests across 18 suites, typecheck and lint clean.

Task 15: implemented, DONE (commit 3860e78; 338 passing across 18 suites, up from 332; typecheck + lint clean). All four amendments applied. SIMULATOR: `npm run ios` DID run on a booted iPhone 17 Pro — the app bundled and rendered the Stage-0 tutorial screen, screenshot confirmed. That is meaningful beyond a smoke test: the tutorial being the first screen on a fresh install is direct evidence that Task 13's gate works in the real app, not just under RTL. Could NOT tap through interactively (no idb/cliclick; AppleScript clicks did not register on sim content after 3 bounded attempts), so tutorial completion, fade behaviour and on-device nav taps remain unverified by hand — reported honestly rather than claimed. Navigation and reset are covered by RTL component tests instead. Review dispatched (base 2246a7c, head 3860e78).
REMAINING MANUAL GAP TO SURFACE TO THE USER: nobody has tapped through this app by hand. Worth one manual pass before or alongside the TestFlight build.

Controller pre-dispatch trace of Task 15 (3 tests): reset-confirmation flow and the saveProgress(emptyProgress()) assertion are sound. One defect: ruling R5's FOURTH and final occurrence — `toContain(12)` against the string "12 days practised"; amend to toContain('12 days').
CRITICAL WIRING NOTE for Task 15: __tests__/today-session.test.tsx mocks expo-router with ONLY `Redirect` exported. The moment app/index.tsx imports `Link` (or anything else) from expo-router to satisfy the navigation requirement, that mock must be extended or the test breaks. Told the implementer explicitly.

Task 14: implemented, DONE (commit 2246a7c; 6/6 new, full suite 332/332, typecheck + lint clean). R5 amendment applied. Implementer verified the mental-vs-reflex conjunction via a trap case and that all 180 cells render unfiltered. Review dispatched (base a670d3c, head 2246a7c).
DEFERRED MINOR FOR THE FINAL WHOLE-BRANCH REVIEW — act() warning noise from ProgressProvider during full-suite runs. The Task 14 implementer found it unprompted and confirmed by stash/restore that it exists on BASELINE, i.e. it came in with Task 12's provider, not with Task 14. Test output noise is a finding under the review rubric, and act() warnings specifically indicate state updates outside act(), which can mask genuine async issues. Origin is almost certainly the hydration effect's setState resolving after the test's synchronous body. Point the final review at this explicitly.

Controller pre-dispatch trace of Task 14 (6 tests): cellState returns unseen/learning/reflex/mental correctly at each boundary — note test 3's 5-entry latency array still clears the now-3 LATENCY_WINDOW; 180 cells render; grid indexing is guarded for noUncheckedIndexedAccess. One defect: ruling R5 applies A THIRD TIME — `expect(getByTestId('atom-summary').props.children).toContain(0)` is a number against the string "0 of 180 moves are mental". Amend to toContain('0 of 180').

Task 13: implemented, DONE (commit a670d3c, 7 files +152/-2; 326/326 up from 321, typecheck + lint clean). All three amendments applied. No-loop trace reported: Today redirects only while !tutorialDone; completeTutorial sets state AND awaits saveProgress before its .then() navigates home; ProgressProvider is a single root-level instance so Today re-reads tutorialDone=true from the same context on return, and the flag is persisted, so even a mid-navigation crash cannot reproduce the loop. Redirect tested via a test-file-scoped jest.mock('expo-router') stub — <Redirect> throws outside a real nav tree — letting the test assert the actual redirect TARGET rather than just absence of session UI; production code untouched. Handed to the reviewer to judge whether the stub masks anything. Implementer also documented a TS 6.0.3 narrowing quirk (bare api?.foo after a closure-only reassignment narrows to `never`; wrapped in waitFor per the file's existing idiom).

Ruling: T13-PRE-1 — the two halves of the tutorial gap are MUTUALLY DEPENDENT and must land together. The brief's app/tutorial.tsx calls only router.replace('/') without persisting tutorialDone. So adding the redirect gate ALONE would produce an infinite loop: Today redirects to the tutorial, the tutorial completes and returns to Today, tutorialDone is still false, redirect again, forever. Implementing either half alone is worse than neither. Ruling: Task 13 adds (a) `completeTutorial()` to the ProgressProvider API, which sets tutorialDone and persists it, (b) the tutorial calling it before navigating home, and (c) the gate in app/index.tsx via expo-router's <Redirect>, not an effect. Also requires updating Task 12's __tests__/today-session.test.tsx, whose mock returns emptyProgress() with tutorialDone FALSE — those tests would otherwise break the moment the gate lands, which is itself proof the gate was missing. — cost if wrong: a returning learner who somehow loses the flag repeats a ten-rod tutorial once.
Ruling: T13-R5 applies (from the pre-flight scan): two ReadingDrill tests assert toContain(2) / toContain(1) — numbers against a string receiver, which Jest rejects. Use 'Rod 2' / 'Rod 1'.

Ruling: T12-Important-1 (plan-mandated) — FIX. `attempt` assigns latest.current INSIDE the setProgress updater. React 19 batches, so the updater runs in the render phase after submit() returns — but SessionRunner fires onAttempt and onBlockEnd synchronously within the same submit(), no render between. So flush() reads a pre-attempt latest.current and the attempt that ENDED the block is missing from that block's own flush. Self-heals on the next flush, so nothing is lost in a normal session, but it breaks the brief's stated "a crash costs at most one block" bound, and assigning a ref inside a state updater is a render-phase side effect — exactly what react-hooks v7's purity rules exist to catch. Fix: compute next synchronously from latest.current, assign the ref, setState with the value not a functional updater. Required a test that calls attempt then flush within one act and asserts the saved progress includes the attempt, verified red against the old form. — cost if wrong: none identified; the ref is the source of truth either way and same-tick attempts still compose.
Ruling: T12-⚠️1 — RESTORE `screenOptions={{ headerShown: false }}` to app/_layout.tsx. Task 1's placeholder had it; the brief's snippet omitted it, so it was dropped silently rather than decided. A five-minute drill should not carry navigation chrome, and nothing in Phase 1 navigates via that header. — cost if wrong: no header bar, which is what a full-screen practice app wants.
Task 12: minor (deferred): useState(() => Date.now()) captures at mount rather than at memoisation — real but bounded by AsyncStorage read latency and immaterial against dueAt's minute-to-day granularity. The purity rule leaves no better option.

REQUIREMENT CARRIED TO TASK 15 (second cross-task coherence gap): NOTHING NAVIGATES BETWEEN SCREENS. Task 14 adds app/progress.tsx and Task 15 adds app/settings.tsx as routes, but no screen links to them, and with headerShown false there is no chrome to reach them either. Phase 1 would ship with the atom map — its ONLY milestone system, per the T8 spec analysis — unreachable. Task 15 must add explicit navigation affordances from Today to Progress and Settings.

Task 12: implemented, DONE (commit 4423025; 320/320 = 315 prior + 5 new, typecheck + lint clean). The exhaustive-deps disable WAS genuinely needed. Three tooling reconciliations disclosed, all handed to the reviewer: (1) jest.setup.js gained the official AsyncStorage jest mock because automocking @/storage/progressStore crashed on the real native module — this is GLOBAL and could shadow Task 9's own local jest.mock factory, so the reviewer must confirm Task 9's assertions still test what they claim; (2) app/index.tsx captures the clock via useState(() => Date.now()) rather than calling Date.now() inline in useMemo, to satisfy eslint-plugin-react-hooks v7's new `purity` rule — note this evaluates at FIRST RENDER, before hydration completes, whereas the brief's inline call would evaluate when the plan is memoised; (3) two scoped eslint-disables for the new `globals` rule over the standard RTL outer-variable capture idiom. This repo had not hit react-hooks v7's purity/globals/immutability rules before now — relevant to Tasks 13-15 if they do render-time Date.now() or similar.

Controller pre-dispatch trace of Task 12: ProgressProvider's 3 tests and today-session's 2 all pass against the implementation, including under the AMENDED time-budgeted runner — after one answer the focus block still has time, so the prompt correctly remains. The `let api: ... | null` capture pattern compiles: TS narrows api to null at the assertion site (closure assignments are not tracked), so `api?.x` types as undefined, which is legal. No logic defect found.
REQUIREMENT CARRIED TO TASK 13 (coherence gap spanning tasks, not a defect in either): NOTHING ROUTES A NEW LEARNER TO THE TUTORIAL. Task 13 builds app/tutorial.tsx and sets tutorialDone, and Progress carries the flag, but Task 12's Today screen never checks it — so a brand-new learner goes straight to arithmetic atoms without ever being taught to read a rod, which is stage 0 of the curriculum and the prerequisite for everything else. Task 13 must add the gate to app/index.tsx (redirect to /tutorial when !progress.tutorialDone) and must actually persist tutorialDone on completion, which the brief's ReadingDrill does not currently do — it only calls router.replace('/').
Risk flagged for Task 12's dispatch: app/index.tsx carries an `eslint-disable-next-line react-hooks/exhaustive-deps`; if the config has reportUnusedDisableDirectives enabled and the rule would not have fired, the disable itself becomes a lint error.

Task 11: review spec ✅. Reviewer verified cumulative deadlines concretely (empty warmup 10s + focus 10s: at t=15s the block correctly has NOT ended, only past cumulative t=20s does onBlockEnd('focus') fire, and 'warmup' never fires), traced the dropped-atom-survives-cycling path through liveItems and the refill, and confirmed no setInterval/setTimeout anywhere in the advancement path. Also credited the implementer with FIXING a latent bug in my brief's reference code — it dropped fade on any wrong answer regardless of coaching; the implementation correctly restricts it to 'silent', since dropping fade during a demo-coached item is meaningless when the beads are already visible. The unrequested early block-advance was judged sound and verified to fire only when every live item has PERMANENTLY failed out, not when merely requeued.

Ruling: T11-Important-1 — FIX. testID "session-summary" is shared across three branches: the real close screen, the no-close-block fallback, and the empty-queue invariant guard. Both fallbacks render a dead-end View with no finish-button and are indistinguishable by testID from a legitimate close screen, so a future change breaking the findActiveBlock/refill invariant would strand the learner on a blank screen AND no test would catch it (a test asserting session-summary renders would still pass). For a five-minute daily habit, a dead-end with no visible cause is how the habit quietly ends. Fix: distinct testIDs per fallback, plus a test asserting the legitimate close path renders session-summary AND a finish-button so a fallback usurping that testID now fails. Explicitly NOT requiring tests that reach the guard branches — they are invariant guards, and scaffolding to reach genuinely unreachable states is waste; distinguishability is the point. — cost if wrong: two extra testIDs that never appear in a passing run.
Task 11: minor (deferred): plan.totalSeconds unused by the runner — decorative from its POV, since cumulative deadlines derive from the individual block seconds, which is correct.
Task 11: fix round 1/5 dispatched (1 Important open: shared fallback testID). Fix base 96612b5.

Task 11: implemented, DONE (commits 7c6038d + 96612b5; 314/314 with 12 new SessionRunner tests, typecheck + lint clean). Cycling cap DECLINED by the implementer with sound reasoning I accept: the time budget is the intended limiter, and since nothing auto-advances, reps are mechanically bounded by real submits rather than a runaway loop. Implementer rewrote the brief's "3 failures → onFinish" test (under time-budgeting an exhausted queue no longer means session end) to assert the dropped atom's attempt count freezes at 3 across many further cycles — a better test than the original. It also added an UNREQUESTED early block-advance when every item in a block fails out before its deadline, justified as the same no-idle-screen principle as empty-block skipping, and tested; handed to the reviewer to judge whether it can end a block prematurely when failures would otherwise have been requeued and retried in the remaining time. Review dispatched (base 37e0c86, head 96612b5). Told the reviewer this component was written from prose, not transcribed, so there is no reference code to diff against — judge on merit.

Ruling: T11-PRE-1 (LARGEST amendment of the run; found by controller tracing Task 11 before dispatch) — the brief's SessionRunner HAS NO TIMER. It is purely queue-driven: advances on submit, ignores every block's `seconds`, ends when the queue empties. With ~18 items at a few seconds each a "five-minute" session finishes in ~90s, making SessionBlock.seconds, SESSION_SECONDS=285 and the whole block structure decorative. This contradicts the plan's own prose for this component ("SessionRunner — walks a SessionPlan, owns the timer") and spec §6's central promise ("The 5 minutes are fixed and block-structured... you should be able to trust that '5 minutes' means 5 minutes"), which is the design's stated reason the habit survives a bad day.
Considered and rejected: shipping queue-driven and deferring the budget to Phase 2. Rejected because the approved spec makes fixed duration the product's core predictability claim, and because 90 seconds is not the thing the user asked for.
Ruling: implement a time budget with CUMULATIVE deadlines measured from session start, so an empty block's time rolls into the next automatically rather than shortening the session (this also discharges the requirement carried from T8). Items within a block CYCLE until its deadline — which is pedagogically right, not a hack: ~30 reps of 2 new atoms across 120s is exactly how reflex is built. Advancement is evaluated ON SUBMIT using the injected `now`, never on an interval tick, so all logic stays synchronously testable with no fake timers; any countdown is presentation only. The close block is the terminal summary shown once the practice budget is spent, ended by acknowledgement rather than by forcing 30s of staring at it. — cost if wrong: practice time is 255s (45+120+90) plus the close screen rather than exactly 285s, and a learner who answers very slowly gets fewer reps than planned rather than an overrun.

Ruling: T10-RNTL-version — the 13.3.3 pin STANDS despite diverging from learning-database's 14.0.1. Reviewer independently verified that Tasks 11 and 13's briefs also call render() synchronously, so adopting 14.x (async render) would require amending three separate task briefs' test code — out of scope for one task — and confirmed learning-database genuinely uses `await render(...)` throughout, so the divergence is explainable rather than accidental. — cost if wrong: the two repos use different RNTL majors, so test idioms do not transfer between them; revisit if learning-abacus ever shares test helpers with a sibling.

Ruling: T10-Important-1 — FIX. react-test-renderer was added as `^19.2.3`, a range, though the report called it pinned. A future npm update or lockfile regen can resolve a 19.x whose peer is react ^19.3.0, reproducing the exact ERESOLVE conflict the dependency was added to fix. Also diverges from this project's own convention (react and react-native are both exact, no operator). Fix is a one-token change plus a targeted re-resolve — explicitly NOT a wholesale lockfile regen, which would churn hundreds of unrelated entries and make the diff unreviewable. — cost if wrong: react-test-renderer stops floating and needs a manual bump when React does; that is the intended behaviour.
Task 10: minor (deferred): visualForFade called twice (Abacus for the frame, FadeLayer for opacity). Cannot diverge — pure calls on the same prop in the same render pass. The brief's PROSE is what is loose, not the code.
Task 10: fix round 1/5 dispatched (1 Important open: caret instead of exact pin). Fix base 5e3e769.

Task 10: implemented, DONE (commit 5e3e769; 302/302 across 11 suites, typecheck + lint clean). Fade containment self-check passed — grep confirms FadeLevel/visualForFade appear only in FadeLayer.tsx and Abacus.tsx, never in Bead.tsx/Rod.tsx. Two dependency decisions to review: @testing-library/react-native pinned to 13.3.3 rather than 14.0.1 (14.x switched render() to an async backend incompatible with the brief's sync test code) — note this DIVERGES from learning-database, which uses 14.0.1; and react-test-renderer pinned exactly to 19.2.3 because auto-resolution pulled 19.3.0 and reconflicted with react@19.2.3. Implementer also self-reported that visualForFade is called twice (Abacus for the frame, FadeLayer for opacity) rather than once as the brief's prose says; code is verbatim, both derive from the same fade prop so they cannot diverge. All three handed to the reviewer to judge. Review dispatched (base 90ec126, head 5e3e769; diff is 257KB but almost entirely package-lock).

Task 9: complete (commits 130725c..90ec126, review clean). Fix round 1/5 (1 addressed, 0 open; commits a17d3d7..90ec126). Re-review enumerated all six Progress fields and confirmed each is guarded, that lastSessionDay's legitimate null survives (the over-strict trap), that the remaining `parsed as Partial<Progress>` is benign narrowing after a typeof guard rather than the old unsoundness renamed, and that the missing-tutorialDone test proves the document was KEPT (daysPracticed still 3). 293 tests green.

Controller pre-dispatch trace of Task 10 (9 tests): BEAD_OPACITY solid 1 / frame 0 / hidden 0 and monotonic solid>dim>ghost ✓; showsFrame true at 'frame', false at 'hidden' ✓; FadeLayer style opacity 1 at F0 and 0 at F6 ✓; Abacus renders 2 rods, testID flips abacus-frame→abacus-blank between F5 and F6, accessibilityValue reports 4 and 7 for setValue(…, 47) ✓. Logic sound, no amendment. Risk flagged to the implementer rather than the logic: @testing-library/react-native needs a matching `react-test-renderer` peer under React 19, which may need installing alongside it — pre-authorised.

Ruling: T9-Important-1 (plan-mandated) — FIX, do not park. loadProgress checked only schemaVersion, then spread the stored object over the defaults and cast `as Progress`. A version-matching document with `atoms: "garbage"` or a string daysPracticed survives the guard; the cast hides it; Task 12 feeds progress.atoms straight into the session planner, so the crash lands in the module with NO fallback rather than in the one whose whole job is to absorb it. Fix: build the result field by field, lenient on missing fields (default, as the spread already did) but strict on wrong-typed ones. The `as Progress` cast must disappear — if it is still needed, something is still unchecked, which makes the cast the completeness test for the fix. Also tightened the four fallback tests, which asserted only that emptyProgress() came back — something a broken mock would also produce — by additionally asserting getItem was called with STORAGE_KEY. — cost if wrong: a stored document with one bad field silently loses that field's history instead of the whole document; acceptable, and strictly better than propagating it.
Task 9: minor (deferred): no validation of the INTERIOR of each AtomRecord — a malformed record inside a well-shaped atoms map still reaches the scheduler. Deeper than this task warrants; recorded rather than built.
Task 9: review spec ✅, no Critical. Reviewer verified byte-identical transcription programmatically and settled the mock-interop question empirically by running babel-preset-expo through @babel/core — the default import resolves to the real mock, and a broken interop would fail loudly at mockResolvedValue rather than pass silently. Suite judged adequate.
Task 9: fix round 1/5 dispatched (1 Important open: stored-document shape unvalidated). Fix base a17d3d7.

Task 9: implemented, DONE no concerns (commit a17d3d7; 6/6 new, 290/290 total, typecheck + lint clean). NEITHER pre-authorised env fix was needed — the `@/` alias resolved under jest without a moduleNameMapper, and lint accepted the commented empty catch. Review dispatched (base 130725c, head a17d3d7). Flagged to the reviewer: four of the six tests assert emptyProgress(), which is also what a BROKEN AsyncStorage mock would produce (undefined getItem → throw → caught → emptyProgress), so only the round-trip and setItem tests genuinely discriminate; and loadProgress's `{...emptyProgress(), ...candidate} as Progress` validates only schemaVersion, so a stored doc with a malformed `atoms` field would pass straight through to Task 12's session planner.

Controller pre-dispatch trace of Task 9 (all 6 tests): null → emptyProgress ✓; stored doc round-trips daysPracticed 7 via {...emptyProgress(), ...candidate} ✓; malformed JSON caught ✓; wrong schemaVersion discarded ✓; rejected getItem caught ✓; setItem called with the versioned key ✓. Logic is sound. TWO ENVIRONMENT RISKS flagged to the implementer rather than the logic: (a) this is the FIRST test in the repo to import via the `@/` path alias — every earlier domain test used relative paths — and jest does not read tsconfig `paths` by default, so `@/domain/progress` will likely fail to resolve until a moduleNameMapper is added; pre-authorised. (b) saveProgress's catch block is empty apart from a comment, which some no-empty lint configs reject; pre-authorised to satisfy the rule without swallowing the error silently.

Ruling: T8-Important-1 (plan-mandated) — FIX, do not park. faderep filtered on "seen && fade < MAX_FADE" with NO fluency gate, so an atom with one attempt (box 2 of 5) gets pushed a visibility level up in the learner's first session. Spec §6 says fade rep draws on FLUENT atoms and §4 gives the principle — fade advances only when fluency is high, "so the image is always almost there — the condition under which the mind supplies it." Presenting an unfamiliar atom with less visual support is exactly the guessing-blind failure the fade design exists to prevent. Spec is the binding authority and the brief contradicts it. Fix: gate on isReflex(record, classify(atom), calibrationMs) — the same predicate the unlock gate uses. Note this makes the existing conditional fade-rep test go VACUOUS (its one-attempt record no longer qualifies), so the test must be strengthened to an unconditional assertion in the same round; the two issues cannot be fixed separately. Also added a test pinning the gate, with deliberate-removal verification. — cost if wrong: fade rep starts a few sessions later than it otherwise would, and a brand-new learner's fade-rep block is empty on day one.

Ruling: T8-cross-block-duplication — NO ACTION. The reviewer flagged that an atom can appear in both focus (at its own fade) and faderep (one level up) within one session. With the fluency gate in place I judge this acceptable and arguably good: drilling an atom, then drilling it harder later in the same five minutes, is reasonable within-session spacing. — cost if wrong: the learner sees the same atom twice in one session, which reads as repetitive rather than as progression.

REQUIREMENT CARRIED TO TASK 11: the reviewer flagged, and the fluency gate makes more likely, that a block can legitimately have zero items — warm-up on day one, and now fade rep until the first atom becomes fluent. selectSession's contract is item selection only, so the runner decides what an empty block does. It must NOT silently consume its 45s/90s on an idle screen, nor collapse and shorten the session below the 285s the learner was promised. Task 11's dispatch must state this explicitly.

Task 8: minor (deferred): Math.max(FOCUS_ITEMS, fresh.length) is dead logic — fresh caps at 2, FOCUS_ITEMS is 3, so it always returns 3.
Task 8: minor (deferred): totalSeconds references SESSION_SECONDS rather than summing the blocks; test 3 cross-checks both directions so drift is still caught.
Task 8: minor (deferred, from reviewer ⚠️): the "only offers atoms from an unlocked stage" test only uses emptyProgress (a single-stage pool), so multi-stage `available` accumulation is never exercised against a wrongly-included locked stage.
Task 8: fix round 1/5 dispatched (1 Important open: fade rep lacks a fluency gate). Fix base 1041054.

Task 8: implemented, DONE no concerns (commit 1041054; 283/283 = 274 prior + 8 session + 1 parameterized purity case; typecheck + lint clean). Empirical finding I requested: the conditional fade-rep assertion DOES execute — instrumented run observed item.fade=1, record.fade=0 — so it is real coverage today, not decoration. Instrumentation removed and the committed test confirmed byte-identical to the brief. It stays fragile though: one planner change that empties the block and it goes silent without failing. Handed to the reviewer to rate rather than pre-judged. Review dispatched (base c5112a6, head 1041054), also asking it to judge cross-block atom duplication and whether a mid-progression learner can get an empty warm-up block.

Controller pre-dispatch trace of Task 8 (all 8 tests): blocks 45+120+90+30 = 285 = SESSION_SECONDS ✓; currentStage(empty) = 1 so available = stage 0 (empty) + stage 1 (50 direct atoms) ✓; new learner gets warmup [] and focus of exactly 2 ✓; faderep presents record.fade + 1 ✓. No defect found — dispatching unamended. Two observations to hand the reviewer rather than fix: (a) the fade-rep test is wrapped in `if (item !== undefined && record !== undefined)`, so it passes VACUOUSLY when the block is empty — same assert-nothing shape as the earlier lint/format cases; (b) `Math.max(FOCUS_ITEMS, fresh.length)` is a no-op since FOCUS_ITEMS is 3 and fresh caps at NEW_ATOMS_PER_DAY = 2.
Incidental property worth recording: because ATOMS is generated rodValue-then-operand, a brand-new learner's first two atoms are '0+1' and '0+2' — easiest-first by accident of generation order rather than by design. Pedagogically fine, but nothing enforces it.

Ruling: T7-Important-1 (plan-mandated) — FIX, do not park. dayKey used `toISOString().slice(0,10)`, i.e. the UTC calendar date, but daysPracticed is the app's headline metric and Phase 1's ONLY milestone. At UTC+9 the day rolls at 9am local, so practising Monday 10pm and Tuesday 8am counts as one day, while Monday 8am and Monday 10am counts as two — it both under- and over-counts in ordinary use. The user is in Japan, so this is live, not theoretical. Fix: use getFullYear/getMonth/getDate (device-local), keeping the YYYY-MM-DD format and signature since markDayPracticed compares these strings and Task 12 calls dayKey(Date.now()). Also required a test that genuinely fails against a UTC implementation — the existing test only checks the string format and would pass either way — verified by temporarily reverting and confirming red. — cost if wrong: a learner who crosses timezones mid-habit sees a day boundary shift with them, which is the correct behaviour for a personal habit app anyway.

Task 7: review spec ✅, quality Approved. Reviewer independently re-derived the calibration trail line by line and confirmed [900,900,400,400,400]; noted that without Amendment 2 it would read [900,900,400,400,900].
Task 7: minor (deferred): classFor re-spells the AtomClass union instead of importing it — will drift silently if AtomClass changes.
Task 7: minor (deferred): recalibrate duplicates medianLatencyMs's sort/mid logic instead of sharing a median helper.
Task 7: minor (deferred): no test covers recalibrate with 2+ direct atoms (even-length averaging), non-direct exclusion, or classFor's throw on a malformed id.
Task 7: brief inconsistency noted by reviewer (no action): the brief's Consumes list names ATOMS and CLASS_TARGET_MS, which neither the brief's own code nor the implementation imports.
Task 7: fix round 1/5 dispatched (1 Important open: dayKey UTC boundary). Fix base bc2be32.

Task 7: implemented, DONE no concerns (commit bc2be32; 11/11 new, 273/273 total, typecheck + lint clean). Both amendments applied. Recalibration test produced calibrationMs 400 with the sequence [900, 900, 400, 400, 400] — exactly the controller's predicted trace, holding at 400 through the attempt-5 fade promotion instead of reverting to the default. Review dispatched (base eae324f, head bc2be32). Asked the reviewer to re-derive that sequence independently rather than accept it, and to judge dayKey's UTC boundary on merit for a daily-habit app.

Task 5: complete AGAIN after reopening (commits 77dc8c9..eae324f across two fix rounds, review clean). Fix round 2/5 (2 addressed, 0 open; commits aa96885..eae324f). Re-review traced D1 step by step: window now reaches 3 at attempt 3, two attempts before the promotion wipe at attempt 5, and isReflex returns true at fade 1 — structurally fixed for every fade level. D2 traced: 5x slow-correct leaves cc at 0 and fade at 0 while box still climbs. Clear-on-fade-change survived. 261 tests green. Implementer independently traced both defects and agreed with the window=3 choice before implementing.

Ruling: T7-PRE-2 (found by controller tracing Task 7 against the AMENDED scheduler) — the brief's test `recalibrates once a direct atom has a full window` STILL cannot pass, for a new reason. Trace with LATENCY_WINDOW=3: attempts 1-2 leave the window short; at attempt 3 it fills and recalibrate() sets calibrationMs to 400; attempt 4 keeps it; at attempt 5 consecutiveCorrect hits 5, the fade promotes, the window is cleared — and recordAttempt then calls `recalibrate(atoms, DEFAULT_CALIBRATION_MS)`, which finds no atom with a full window and SNAPS CALIBRATION BACK TO 900. The test asserts 400 and would get 900. Worse than the test: in real use, every fade promotion would discard the learner's measured speed and reset their targets to the factory default. Ruling: amend recordAttempt to pass `progress.calibrationMs` as recalibrate's fallback instead of DEFAULT_CALIBRATION_MS, so calibration persists across window clears and only DEFAULT seeds a genuinely fresh Progress. Also pass progress.calibrationMs through to applyAttempt for its new 6-arg signature. — cost if wrong: a stale calibration lingers after a long layoff instead of resetting; the spec's §8 recalibration-after-absence work (deferred to Phase 2) is where that would be addressed properly.

Note: checked whether the clamp makes the direct-class target degenerate once calibrated. latencyTargetMs('direct', cal) = 900 * clamp(cal/900, 0.6, 2.5). With cal=400 the raw scale 0.444 clamps to 0.6, giving a 540ms target rather than 400 — so a learner is not required to beat their own median. The clamp's lower bound is load-bearing, not decorative.

Ruling: T7-PRE-1 (SYSTEMIC — found by controller tracing Task 7 before dispatch; spans Tasks 5, 6, 7) — FIX, reopening completed Task 5.
Defect D1: LATENCY_WINDOW (5) equals FADE_PROMOTE_STREAK (5), so the 5th consecutive correct answer promotes the fade and clears recentLatencyMs on the SAME applyAttempt call. Traced: after any clear, the window reaches at most 4 entries before the next promotion wipes it. Therefore isReflex (which requires length >= LATENCY_WINDOW) is UNREACHABLE below fade 6. Knock-ons: Task 6's unlock gate (isReflex AND fade>=3) silently collapses to "fade == 6"; recalibrate() never sees a full window so calibrationMs is pinned at DEFAULT_CALIBRATION_MS forever, making the "targets adapt to the learner" design a no-op; and Task 7's test `recalibrates once a direct atom has a full window` cannot pass.
Defect D2: nextFadeLevel counts ANY correct answer toward promotion, ignoring latency. Contradicts plan §7 ("five consecutive correct within the latency target") and spec §4 ("fade advances only when fluency is high at the current level"). As built the beads fade out on slow-but-correct answers, undermining the core mechanism.
Ruling: set LATENCY_WINDOW = 3 (keeping FADE_PROMOTE_STREAK = 5) so the window refills between promotions while still only ever holding timings taken at the current fade; and gate consecutiveCorrect on speed, which requires applyAttempt to take calibrationMs and actually use its `cls` parameter (currently `_cls`, unused). Keeping the clear-on-fade-change behaviour — it is principled: carrying fast timings from an easier fade into a harder one would let atoms rocket up the ladder on stale evidence. — cost if wrong: isReflex is judged on 3 samples instead of 5 (noisier), and an atom briefly shows non-reflex for two attempts after each promotion; both are population-level acceptable across a 50-atom stage, and reverting means changing one constant back.

Task 6: implemented, DONE no concerns (commit aa96885; 9/9 curriculum, 259 total, typecheck + lint clean). Correction to my pre-dispatch trace: the brief has 9 tests, not 10 — I had counted the two assertions inside 'always unlocks stages 0 and 1' as separate cases. 249 + 9 + 1 parameterized purity case = 259, consistent. Review dispatched (base d556405, head aa96885). Asked the reviewer to judge the empty-previous-stage early return in isStageUnlocked and whether atomsForStage's repeated re-derivation of all 180 decompositions is worth flagging.

Task 5: complete (commits 7e63786..d556405, review clean). Fix round 1/5 (1 addressed, 0 open; commits 77dc8c9..d556405). Re-review hand-traced all five loop iterations and confirmed the new test genuinely enters the fadeChanged branch on iteration 5; +12/-0, fluency.ts untouched. 249 tests green.

Controller pre-dispatch trace of Task 6 (all 10 tests vs the brief's implementation): STAGES 0-4 ✓; the 4 stage classes partition all 180 atoms exactly once ✓; isStageUnlocked returns true for index <= 1 ✓ and false for index 2 with empty records (0/50 < 0.85) ✓; allReflex(1) gives 50/50 with box 5, fade 4, median 300 < target 900 → unlocked ✓; fade 2 < UNLOCK_MIN_FADE 3 keeps it locked ✓; highestUnlockedStage breaks at the first locked stage, giving 1 for empty and 2 for allReflex(1) ✓. No defect found — dispatched unamended.

Ruling: T5-Important-1 (plan-mandated, and a direct cost of my own ruling T5-PRE-1) — FIX, do not park. Seeding the latency-window test at fade 6 removed the only case that would have fired applyAttempt's fade-change reset, so clearing consecutiveCorrect/consecutiveWrong/recentLatencyMs is live behaviour with zero coverage. Buying the hole back with one test that asserts all four properties after a promotion. — cost if wrong: one extra test; none if right.

Ruling: T5-⚠️1 — the reviewer could not verify whether Tasks 6/7/14 can call latencyTargetMs/isReflex with calibrationMs <= 0. Resolved by the controller from the plan: Task 7's progress.ts defaults calibrationMs to DEFAULT_CALIBRATION_MS (900) and recalibrate() returns that fallback when no atom has a full window; latencies themselves come from SessionRunner's `Math.max(1, ...)`, so the value is always >= 1. The clamp floors the scale at 0.6 regardless. No action; carry a note into Task 7's dispatch so its reviewer can confirm the default actually lands as planned. — cost if wrong: a nonsensical-but-bounded latency target, never a crash.

Task 5: review spec ✅, quality Approved. Reviewer confirmed fluency.ts byte-identical to the brief, the amendment applied narrowly (only added import was `type AtomRecord` for the seed), and latencyTargetMs's clamp order robust to pathological input by trace.
Task 5: minor (deferred): no test exercises latencyTargetMs/isReflex with calibrationMs <= 0 (implementation handles it safely).
Task 5: fix round 1/5 dispatched (1 Important open: fade-change reset untested). Fix base 77dc8c9.

Task 5: implemented, DONE no concerns (commit 77dc8c9; 248/248 — 13 new fluency tests including the amended latency-window one; typecheck + lint clean). Ruling T5-PRE-1 applied as instructed; T5-PRE-2 not needed — `_cls` did not trigger no-unused-vars under the existing config, so eslint.config.js was untouched. Review dispatched (base 7e63786, head 77dc8c9), with the amendment stated as a requirement so a `fade: 6` seed is not read as drift. Asked the reviewer to check whether the fade-change reset is now untested (the amendment routed the only test that would have fired it away from firing it) and whether latencyTargetMs handles a zero/negative calibration.

Task 4: complete (commits 001d9b8..7e63786, review clean). Spec ✅, Approved, zero Critical/Important. Reviewer judged both flagged items sound: the `as FadeLevel` casts are provably in-domain after clamping (TS widens literal-union arithmetic to number, so the cast is unavoidable), and demote-before-promote is safe because the two streak counters are mutually exclusive by construction.
Task 4: minor (deferred): no test exercises the simultaneous promote+demote streak case (gap in the brief's own test file, not reachable from real call sites).

Ruling: T5-PRE-1 (found by controller before dispatch, not by a reviewer) — the brief's test `keeps only the last five latencies` CANNOT PASS against the brief's own `applyAttempt`. Trace: six correct attempts on a fresh record; at attempt 5 `consecutiveCorrect` hits FADE_PROMOTE_STREAK, so `nextFadeLevel(0,5,0)` returns 1, `fadeChanged` is true, and the implementation deliberately clears `recentLatencyMs` to []. Attempt 6 then leaves `[600]`, not the asserted `[200,300,400,500,600]`. Both behaviours are individually correct — a fade promotion makes the atom a different exercise, so stale latencies must not carry over — but the test as written trips the promotion it did not intend to exercise. Ruling: amend the TEST, not the implementation; seed the record at `fade: 6` (MAX_FADE), where `nextFadeLevel` cannot promote, so the six attempts accumulate and the slice(-5) behaviour is what actually gets asserted. — cost if wrong: the latency-window behaviour ends up asserted only at max fade rather than generally; the reset-on-fade-change behaviour stays untested until someone adds a case for it.

Ruling: T5-PRE-2 — `applyAttempt`'s `_cls` parameter is intentionally unused (kept so callers, notably progress.ts, can pass the class without a signature change). If lint's no-unused-vars flags it, adding `argsIgnorePattern: '^_'` to eslint.config.js is pre-authorised. — cost if wrong: a one-line lint config change that loosens unused-arg checking for underscore-prefixed params only.

Task 4: implemented, DONE no concerns (commit 7e63786; 234 tests = 16 new fade + 217 prior; typecheck + lint clean). Review dispatched (base 001d9b8, head 7e63786). Asked the reviewer to judge two things on merit: the `as FadeLevel` assertions on clamped arithmetic in nextFadeLevel, and whether demote-before-promote precedence is correct or an ordering bug.

Task 3: complete (commits d4d05b0..001d9b8, review clean). Fix round 1/5 (1 addressed, 0 open; commits b6dc354..001d9b8). Re-review confirmed the new assertion uses a hard-coded literal (not derived from the same tally), atoms.ts has zero hunks, and atoms.test.ts is 14 insertions / 0 deletions. 217 tests green. Distribution now confirmed four ways.

Ruling: T3-Important-1 (plan-mandated) — FIX, do not park. classify() is covered by 5 of 180 spot cases while its output is now spec fact (§3) and Tasks 6/7/14 partition curriculum content by class. The 180-case property test checks that decompose's steps SUM correctly, never their SHAPE (step count / carry presence), which is what classify keys off — so a future edit could preserve arithmetic, pass everything, and silently reclassify atoms, desyncing the spec and mis-routing lesson content. Fix is one aggregate test asserting the exact {direct:50, five:40, ten:50, both:40}. No live defect today: three independent derivations (generator tally, controller hand-enumeration, reviewer from-scratch transcription) agree. — cost if wrong: one cheap test that would need updating if the class taxonomy ever legitimately changes.

Task 3: review spec ✅, quality Approved. Reviewer confirmed atoms.ts and atoms.test.ts byte-identical to the brief, ATOMS generated not hand-listed, purity clean, and the §3/§12 spec edits read correctly with no leftover "approximate" language.
Task 3: minor (deferred): Object.freeze(ATOMS) is shallow — individual Atom objects remain mutable; consider per-element freeze or readonly fields if ATOMS is later handed to UI code.
Task 3: fix round 1/5 dispatched (1 Important open: no regression protection on the class distribution). Fix base b6dc354.

Task 3: implemented, DONE no concerns (commit b6dc354; 190/190 atoms.test.ts = 180 property + 10 fixed, full suite 216/216, typecheck + lint clean). All 180 property cases passed first try, no algorithm change. Per-class counts measured: direct=50, five=40, ten=50, both=40. Spec §3 updated with the real figures. Review dispatched (base d4d05b0, head b6dc354).

Controller cross-check of the atom counts (done independently, before accepting the spec edit): enumerated all 90 addition atoms by hand from the algorithm. No-carry direct requires n <= 4-(v%5), PLUS n==5 whenever v<=4 (the zero correction is filtered, leaving one step). Carry atoms are 'ten' iff (10-n) <= v%5 OR n==5, else 'both'. Addition totals: direct 25, five 20, ten 25, both 20. Doubled for subtraction by symmetry = 50/40/50/40. AGREES with the generator. The spec's original estimates (~70/~30/~50/~30) were wrong: there are 130 complement atoms, not 110 — the rewiring workload is larger than the spec claimed, which is worth surfacing to the user at the end.

Task 2: complete (commits 4041003..d4d05b0, review clean). Fix round 1/5 (1 addressed, 0 open; commits 3585cc0..d4d05b0). Re-review verified each bad input (NaN, -1, 470 into 2 rods) now throws, the success path is byte-identical, and soroban.test.ts had 0 deletions so no existing test was adjusted. 25 tests green.

Ruling: T2-Important-1 (plan-mandated) — FIX, do not park. `rodFor`'s guard `value < 0 || value > ROD_MAX` is defeated by NaN (both comparisons false), and `setValue`'s padStart never truncates, so a too-wide value silently grows the soroban and a negative value yields `earth: NaN`. The code is verbatim from my brief, so the defect is the plan's, not the implementer's. No Phase 1 caller can trip it — Tasks 3, 10, 11 and 13 all pass 0-9 or a 2-digit literal — so this is hardening, not a live bug. Fixing anyway: the module's only defensive code currently gives false assurance, and silent NaN state is very expensive to diagnose downstream. Also authorised adding error-path tests beyond the brief's test list (reviewer noted zero coverage of the defensive code). — cost if wrong: one fix round spent hardening a path no Phase 1 caller exercises.

Task 2: review spec ✅, quality Approved. Reviewer verified soroban.ts and soroban.test.ts are byte-for-byte transcriptions of the brief and reconciled the 17-test count (3 + 10 it.each + 4).
Task 2: fix round 1/5 dispatched (1 Important open: setValue/rodFor unguarded width and sign). Fix base 3585cc0.

Task 2: implemented, DONE no concerns (commit 3585cc0; 19 tests pass — 17 soroban + 2 purity; typecheck and lint clean). Review dispatched (base 4041003, head 3585cc0).

Ruling: T1-Important-2 (simulator boot) — the review re-raised the boot-evidence point as Important. Ruling T1-⚠️2 above stands unchanged; it does NOT enter the fix loop. Same reasoning, same cost if wrong.

Task 1: minor (deferred): eslint/dependency pruning went beyond the brief's literal Step 1/2 — later tasks must not assume the stock scaffold's dependency set.
Task 1: minor (deferred): react-dom / react-native-web retained as unused weight pending real peer-resolution confirmation.
Task 1: minor (deferred): `typecheck` cold-starts tsc twice; a composite `tsc -b` is an option if typecheck time grows.

Task 1: complete (commits c7856df..4041003, review clean). Fix round 1/5 (1 addressed, 0 open; commits e6eab88..4041003). Re-review confirmed @stylistic rules fire on deliberate violations and that no existing file trips them.

Task 1: fix round 1/5 dispatched (1 Important open: eslint.config.js does not enforce the mandated no-semicolons/single-quotes/2-space style, so lint cannot hold the constraint across the remaining 14 tasks). Fix base e6eab88.

Task 1: implemented, DONE_WITH_CONCERNS (commit e6eab88; 2/2 tests, typecheck + lint clean, expo export bundles 1103 modules). Disclosed deviations: routes moved src/app/ -> app/; two-invocation typecheck + tsconfig.tools.json + @types/node; eslint.config.js and jest.setup.js contents invented (brief named files, not contents); deps pruned for iOS-only, react-dom/react-native-web kept for a peer conflict. Review dispatched (base c7856df, head e6eab88).

